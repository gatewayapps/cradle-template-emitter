Attachment:
  properties:
    attachedTo: string[]
    availableSizes:
      modelRef: AttachmentSizes
    baseStorageUrl: string
    creator: string
    dateCreated: integer
    dateUpdated: integer
    description: string?
    extension: string
    fileName: string
    id: string
    isPublic: boolean
    mimeType: string
    photoOf: string?
    tags: string[]
    title: string
    type: string allow("Document", "MainPhoto", "NotSpecified", "Photo", "Video")
