User:
  properties:
    allowedAccessTo:
      isArray: true
      modelRef: EntityAllow
    confirmPassword: string
    id: string
    firstName: string
    lastName: string
    memberOf: 
      isArray: true
      modelRef: Membership
    status: string
    title: string?
    username: string
    password: string
    permissions: 
      modelRef: UserPermissions
