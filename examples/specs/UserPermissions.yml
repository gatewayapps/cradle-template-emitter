UserPermissions:
  properties:
    isAdmin: boolean default(0)
    isBrokerageUser: boolean default(0)
    isDevelopmentUser: boolean default(0)
    isGuest: boolean default(0)
    accessAllPipelines: boolean default(0)
    editFeeDistribution: boolean default(0)
    setFeaturedListing: boolean default(0)
    specialPermissions: boolean default(0)
